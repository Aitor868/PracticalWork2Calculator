<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PracticalWork2Calculator.Views.PasswordRecoveryPage"
             Title="Recuperar Contraseña">
    <VerticalStackLayout Spacing="15" Padding="30" VerticalOptions="Center">
        <Label Text="Recuperación de Contraseña" FontSize="Header" HorizontalOptions="Center" />
        <Label Text="Esta es una funcionalidad de marcador de posición." HorizontalOptions="Center" Margin="0,20,0,0"/>
       <Label Text="En una aplicación real, aquí se implementaría un sistema de recuperación (ej. envío de correo)."  HorizontalOptions="Center" HorizontalTextAlignment="Center"/>

        <Button Text="Volver a Inicio de Sesión" Clicked="OnBackToLoginClicked" Margin="0,20,0,0"/>
    </VerticalStackLayout>
</ContentPage>